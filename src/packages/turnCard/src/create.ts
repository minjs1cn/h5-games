import {
  createClassNamespace,
  createComponentNamespace
} from '../../shared/create'

const createClassname = createClassNamespace('turnCard')

const createTurnCard = createComponentNamespace('TurnCard')

const createCard = createComponentNamespace('Card')

export { createClassname, createTurnCard, createCard }
