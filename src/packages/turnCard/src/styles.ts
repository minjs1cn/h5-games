import { createClassname } from './create'

export default {
  group: createClassname('group'),
  card: createClassname('card'),
  cardBack: createClassname('card__back'),
  cardFront: createClassname('card__front'),
  cardActive: '_active'
}
